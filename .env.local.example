# Vercel automatically handles the API URL in production.
# For local development (vite), if you run a separate backend:
# VITE_API_URL=http://localhost:5000/api

# Note: In this Serverless setup, api/index.ts serves /api automatically.
